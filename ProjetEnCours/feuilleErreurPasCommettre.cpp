/*
avec un if ne PAS METTRE de ; après la condition
	if (nombre == 10);

si on ne met pas d'accolade, seulement l'instruction suivante le if est prise en compte

Si le else est souligné en rouge, vérifier pas de ; après les () du if


LNK main() déjà existant : Vérifier si pas deux fichiers avec un main chacun dans le même projet



*/