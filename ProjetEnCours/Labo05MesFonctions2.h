#pragma once				// S'assurer que les fichiers .h ne soient ajoutés qu'une seule fois dans le projet

// Déclaration des prototypes des fonctions : 3 informations : typeDeRetour nomDeLaFonction ()
int calculerCarre(int entier);
int calculerCube(int nb);
double calculerCube(double nb);
