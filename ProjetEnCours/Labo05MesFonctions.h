#pragma once			// Directive du compilateur pour n'inclure qu'une seule fois le fichier .h dans le projet même
						// s'il y a plusieurs #include


void afficherMenu();			// Déclaration de l'existence que la fonction afficherMenu
char validerChoixMenu(char min, char max, char quitter);
